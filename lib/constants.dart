import 'package:flutter/material.dart';

const primaryColor = Color(0xFFF00099);
const secondaryColor = Color(0xFF900000);
const darkColor = Color(0x151580005);
const bodyTextColor = Color(0xFFFFFF80);
const bgColor = Color(0xCCC79659);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 1540.0;
